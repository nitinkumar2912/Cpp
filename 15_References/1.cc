#include <iostream>
int main (){
//  int a{10};
// std::cout<<"The integer name is a = "<<a<<std::endl;
// std::cout<<"The address of a = "<<&a<<std::endl;
// std::cout<<"The size of a = "<<sizeof(a)<<" bytes"<<std::endl;

// std::cout<<std::endl;

// int& A = a;//iam just reffering A to a matlab upar jo a h phle uska nam a tha ab mene uska nam badal dia A se
// std::cout<<"The new name is A = "<<A<<std::endl;
// std::cout<<"The address of A = "<<&A<<std::endl;
// std::cout<<"The size of A = "<<sizeof(A)<<" bytes"<<std::endl;

// std::cout<<std::endl;
// A++;//OR a++ MATLAB A KI VALUE KO +1 KARDEGA

// std::cout<<"The integer name is a = "<<a<<std::endl;
// std::cout<<"The address of a = "<<&a<<std::endl;
// std::cout<<"The size of a = "<<sizeof(a)<<" bytes"<<std::endl;

// std::cout<<std::endl;

// std::cout<<"The new name is A = "<<A<<std::endl;
// std::cout<<"The address of A = "<<&A<<std::endl;
// std::cout<<"The size of A = "<<sizeof(A)<<" bytes"<<std::endl;

// std::cout<<std::endl;

// int b{15};
// A=b;

// std::cout<<"The new name is A = "<<A<<std::endl;
// std::cout<<"The value of b = "<<b<<std::endl;
// std::cout<<"The address of A = "<<&A<<std::endl;
// std::cout<<"The address of b = "<<&b<<std::endl;


//AGAR MERKO ESA KARNA H KI JO M REFFERENCE NAME DUNGA US NUMBER KI HELP S M KHUCH EDIT NAHI KARKSTA TO MUJHE CONST ADD KARNA PADEGA ON REFF
//LIKE M CHATA HU KI AGAR MERKO NUMBER CHANGE KARNA H INT VALUE CHANGE KARNI H TO VO M INT DATA TYPE SE HI CHANGE KARU NAKI REFF KE USE SE

// int c{66};
// int& ref_of_c = c;
// std::cout<<ref_of_c<<std::endl;
// ref_of_c = 55;
// std::cout<<ref_of_c<<std::endl;// like yaha mene reff ki help se value change kari h agar m isko sirf ese karna chau ki value sirf int data type use karke hi change hoye to mujhe const add karna hoga reff ke age

// std::cout<<std::endl;

// int d{77};
// const int& ref_of_d = d;
// std::cout<<ref_of_d<<std::endl;
// ref_of_d = 100; //yaha error agya jese hi mene const laga uske age iska matlab ye sirf read only ke liye h ye khuch modify nahi karskta 
// std::cout<<ref_of_d<<std::endl;



}